#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  network:
    network:
      version: 2
      ethernets:
        ens18:
          dhcp4: true
  ssh:
    install-server: true
    allow-pw: true
  storage:
    layout:
      name: direct
  # Minimal user setup for initial access
  user-data:
    users:
      - name: ansible
        groups: [sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
  # No additional packages - Ubuntu 24.04 Server includes what we need
  packages: []
  # No post-install commands - keep it minimal
  late-commands: []
